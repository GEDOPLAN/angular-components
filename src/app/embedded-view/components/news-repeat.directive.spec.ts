import { NewsRepeatDirective } from './news-repeat.directive';

describe('NewsRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
